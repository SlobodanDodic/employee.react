import React from "react";

const Dashboard = () => {
  return (
    <div>
      If you are using an animation that requires either/both of these elements,
      you need to give the element an ID, and pass that ID to the menu component
      as the pageWrapId and outerContainerId props respectively.
    </div>
  );
};

export default Dashboard;
